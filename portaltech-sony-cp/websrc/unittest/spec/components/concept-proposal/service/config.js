describe('conceptProposalServiceConfig', function conceptProposalServiceConfig() {
    // var service;
    // var services = {};

    // beforeEach(function beforeEach() {
    //     module('conceptProposal');
    //     inject(function injector($injector) {
    //         service = $injector.get('conceptProposalServiceConfig');
    //         [
    //             'sharedDropdownFactoryCore',
    //             'sharedSingleSelectFactory',
    //             'sharedMultiSelectFactory',
    //             'conceptProposalServiceConcept'
    //         ].forEach(function conceptProposalServices(name) {
    //             services[name] = $injector.get(name);
    //         });
    //     });
    // });
});
